module.exports = {
  // Configuración de Obfuscate
  options: {
    // Nivel de obfuscación (0-10)
    compact: true,
    // Eliminar comentarios y espacios en blanco
    compress: true,
    // Encriptar cadenas de texto
    strings: true,
    // Encriptar nombres de variables y funciones
    names: true,
    // Encriptar nombres de propiedades
    properties: true,
  },
  // Archivos y directorios a obfuscate
  files: [
    'src/**/*.js',
  ],
  // Archivos y directorios a ignorar
  ignore: [
    'node_modules/**',
  ],
};